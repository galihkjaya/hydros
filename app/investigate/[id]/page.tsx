export const metadata = { title: "Investigation" };

/**
 * Placeholder. The investigation workspace is built in the next commit; this
 * exists so the form's navigation target resolves.
 */
export default function InvestigationPage() {
  return (
    <main className="mx-auto w-full max-w-6xl px-4 py-14 sm:px-6">
      <h1 className="text-xl font-semibold">Investigation</h1>
      <p className="mt-2 text-muted">Workspace under construction.</p>
    </main>
  );
}
